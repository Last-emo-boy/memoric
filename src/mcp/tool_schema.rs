//! MCP tool schema registration.

use serde_json::{json, Value};

pub(crate) fn register_tools() -> Vec<Value> {
    let mut tools = vec![
        // ═══════════════════════════════════════════════════════════════════
        // 1. GUIDE - Entry point & navigation
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "memoric",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 2. TARGET - Process/thread/module acquisition
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "target",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 3. MEMORY - Core memory operations
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "memory",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 4. INJECT - Code injection & execution
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "inject",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 5. PAYLOAD - PE parsing, obfuscation & lifecycle
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "payload",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 6. HOOK - Function hooking (IAT/inline/hardware)
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "hook",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 7. STEALTH - Evasion, cloaking & self-protection
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "stealth",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 8. DETECT - System reconnaissance & threat detection
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "detect",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 9. PRIVILEGE - Elevation & token manipulation
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "privilege",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 10. KERNEL - Kernel memory & BYOVD operations
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "kernel",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 11. SELF - Introspection & self-test
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "self",
            "inputSchema": {}
        }),
        // ═══════════════════════════════════════════════════════════════════
        // 12. ORCHESTRATE - Auto-orchestration engine
        // ═══════════════════════════════════════════════════════════════════
        json!({
            "name": "orchestrate",
            "inputSchema": {}
        }),
    ];
    crate::mcp::action_registry::enhance_tool_definitions(&mut tools);
    tools
}
